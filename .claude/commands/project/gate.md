Run the full Coruro gate (TypeScript + Rust + Swift) and summarise any failures.

```bash
just gate
```

If `just` is not installed: `brew install just`, then re-run.

Report:
- Which halves passed / failed
- Exact error lines for any failure
- The fix to apply (if clear)
